import 'package:flutter/material.dart';

class AgeSelectScreen extends StatelessWidget {
  const AgeSelectScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Select Age Group')),
      body: const Center(
        child: Text('Age selection workflow coming soon.'),
      ),
    );
  }
}

