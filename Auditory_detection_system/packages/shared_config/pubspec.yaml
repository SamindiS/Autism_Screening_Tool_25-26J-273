name: shared_config
description: Shared backend API config for SenseAI app (root and frontend).
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
